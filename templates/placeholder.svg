<svg width="{{.Width}}" height="{{.Height}}" xmlns="http://www.w3.org/2000/svg">
  <rect x="2" y="2" width="{{.Width}}" height="{{.Height}}" style="fill: {{.Fill}}; stroke: {{.Stroke}}; stroke-width: 2"/>
  <text x="50%" y="50%" font-size="24" text-anchor="middle" alignment-baseline="middle" font-family="sans-serif" fill="{{.Stroke}}">{{.Width}}x{{.Height}}</text>
</svg>
